{
  "name": "ai-office",
  "private": true,
  "packageManager": "npm@10.9.2",
  "workspaces": ["packages/*"],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "dev:server": "npm run dev -w @ai-office/server",
    "dev:web": "npm run dev -w @ai-office/web"
  },
  "devDependencies": {
    "turbo": "^2.3.0",
    "typescript": "^5.7.0"
  }
}
